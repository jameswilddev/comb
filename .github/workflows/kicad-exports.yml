name: KiCAD Exports
on: [push]

jobs:
  make:
    runs-on: ubuntu-20.04
    steps:
    - uses: actions/checkout@v2
    - uses: nerdyscout/kicad-exports@v2.3
      with:
        config: docs.board.kibot.yaml docs.schema.kibot.yaml jlcpcb/plot.green-hasl.kibot.yaml compress.documentation.kibot.yaml gerbers.kibot.yaml gerbers.stencil.kibot.yaml jlcpcb/bom.csv.kibot.yaml jlcpcb/position.kibot.yaml compress.fabrications.kibot.yaml
        schema: 'comb-arcade-breakout-board/comb-arcade-breakout-board.sch'
        board: 'comb-arcade-breakout-board/comb-arcade-breakout-board.kicad_pcb'
    - uses: softprops/action-gh-release@v1
      if: startsWith(github.ref, 'refs/tags/')
      with:
        files: '*.zip'
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
